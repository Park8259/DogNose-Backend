services:
  mysql:
    image: mysql:8.4
    container_name: dog-nose-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: dognose
      MYSQL_USER: dognose
      MYSQL_PASSWORD: dognose1234
      MYSQL_ROOT_PASSWORD: root1234
      TZ: Asia/Seoul
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci

volumes:
  mysql_data:
